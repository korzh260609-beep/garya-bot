name: SG2 Post-Merge Actions Discovery Smoke

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    name: Disabled
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "Disabled: post-merge discovery workflow is no longer part of SG nervous system."
