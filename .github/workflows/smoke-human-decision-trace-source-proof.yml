name: Smoke Human Decision Trace Source Proof

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
      - feat/human-decision-trace-source-proof

jobs:
  smoke-human-decision-trace-source-proof:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install

      - name: Run Human decision trace source proof smoke
        run: npm run smoke:human-decision-trace-source-proof
