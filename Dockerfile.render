FROM ghcr.io/korzh260609-beep/garya-bot-sg22:69c6a315e29a9533ecb86a3acfb35989c734fe8a

# Render runs the exact verified SG 2.2/OpenClaw image. The immutable SHA tag
# prevents Docker cache from restoring an older identity implementation.
# The image's native CMD starts scripts/sg22-render-entrypoint.sh.
