# SG (Советник GARYA)

[![SG Minimal CI](https://github.com/korzh260609-beep/garya-bot/actions/workflows/ci.yml/badge.svg?branch=main)](https://github.com/korzh260609-beep/garya-bot/actions/workflows/ci.yml)

SG (Советник GARYA) — модульный универсальный AI-ассистент и Task Engine-система, построенная по принципу identity-first архитектуры и строгой этапной разработки.

SG проектируется как мультиплатформенная decision-support система.  
Telegram на текущем этапе используется как транспортный слой (transport layer), а не как основа архитектуры.

---

## 🏗 Архитектура

- Node.js (ESM)
- Express — HTTP / Webhook
- PostgreSQL — память, задачи, индексы
- Transport Layer (Telegram — текущая реализация)
- Render — хостинг
- GitHub Actions — CI
- Модульная AI-интеграция

### Ключевые принципы

- Identity-first (монарх определяется только через `process.env.MONARCH_USER_ID`)
- Никакого доверия к `chat_id`
- Чёткое разделение слоёв (Transport / Core / Sources / AI)
- Source-first подход
- Строгая этапность (ROADMAP + WORKFLOW)
- 1 шаг = 1 действие
- Никакой автодеплой без контроля монарха

---

## ⚙ Основные модули

### 🧠 Core / Task Engine

- Создание задач
- Запуск через AI
- Статусы: active / paused / deleted
- Планирование (cron-основа)
- Подготовка к масштабированию через JobRunner

Команды (через текущий transport):

```
/demo_task
/newtask
/task
/tasks
/run <id>
```

---

### 🌐 Sources Layer

Унифицированная система подключения источников данных.

```
/sources
/sources_diag
/source <key>
/diag_source <key>
```

---

### 📂 GitHub Integration

Read-only интеграция с репозиторием:

```
/reindex
/repo_tree
/repo_status
/repo_search
/repo_analyze
/repo_review
/repo_diff
```

Поддерживается snapshot-индексация и безопасный анализ файлов.

---

### 🔐 Identity & Access

Монарх определяется строго через ENV:

```
MONARCH_USER_ID
```

Поддержка access-request workflow:

```
/approve
/deny
/ar_create_test
/ar_list
```

Identity логика не зависит от transport (группа/личка/будущие каналы).

---

## 🚀 CI

Подключён минимальный GitHub Actions workflow:

- Установка зависимостей
- Проверка синтаксиса (`node --check`)
- Автозапуск при push в `main`

CI защищает проект от синтаксических ошибок и нестабильных коммитов.

---

## 🔮 Будущее развитие

SG проектируется как:

- мультиюзерная система
- мультиплатформенный ассистент
- расширяемый AI-оркестратор
- система с модульной памятью
- система с multi-model поддержкой

Telegram — лишь текущая точка входа.

---

## 📌 Статус проекта

Активная архитектурная разработка.  
Развитие по stage-based модели.

---

## 👑 Автор

Monarch: GARYA

---

SG — это не просто бот.  
Это управляемая архитектурная система с контролируемой эволюцией.
