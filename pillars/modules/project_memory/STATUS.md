# project_memory STATUS

> AGENT NOTE:
> This file is intentionally not a progress tracker.
> Do not use it as proof of what currently exists in code, DB, Render, or production.
> For stable agent instructions, read `pillars/modules/project_memory/AGENT_GUIDE.md`.

---

## 1. Purpose

This file exists only as a compatibility pointer for agents or links that still open `STATUS.md`.

Mutable status lists are not maintained here.

Reason:

```text
current state must be verified from the active branch and runtime diagnostics
not from stale documentation checklists
```

---

## 2. Required next action for any agent

Before working on Project Memory:

1. Read `AGENT_GUIDE.md`.
2. Verify current `dev/v2-start` files directly.
3. Verify runtime / Render / DB facts when the task depends on live state.
4. Warn about conflicts before changing anything.

---

## 3. Stable rule

Project Memory documentation describes:

```text
boundaries
contracts
risks
data shapes
operating rules
```

It must not become a constantly edited progress board.

---

## 4. Authority reminder

Authority order remains:

1. Current Monarch instruction.
2. Verified active branch files.
3. Verified runtime diagnostics / Render / DB facts.
4. Approved pillars and module docs.
5. Confirmed Project Memory entries.
6. Current chat context.
7. AI inference.

Project Memory supports continuity.
It does not replace verification.
