# Render Latest

This folder stores the latest sanitized Render diagnostic outputs for quick reading.

Purpose:
- latest status;
- latest logs snapshot;
- latest deploys snapshot;
- latest deploy logs snapshot;
- latest env summary.

Rules:
- generated files only;
- no secrets;
- no env values;
- no Render mutations;
- no source code changes from this folder.

Status:
- skeleton only;
- no writer is connected yet.
