# SG Agent Shared Layer

Shared helpers for SG agents.

Rules:
- shared code must be genuinely shared;
- shared code must not become a hidden agent;
- shared workspace helpers must not perform writes directly in skeleton phase;
- capability-specific logic belongs inside the owning agent.
