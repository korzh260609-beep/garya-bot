// ============================================================================
// === src/repo/textFilters.js — text file validation (SKELETON)
// ============================================================================

export const MAX_TEXT_FILE_SIZE = 200 * 1024; // 200 KB

export function isProbablyText(buffer) {
  // SKELETON: пока считаем, что это текст
  return true;
}

export function isAllowedTextPath(path) {
  // SKELETON: путь уже отфильтрован ранее
  return true;
}

